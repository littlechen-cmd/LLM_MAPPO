# Q2 实验证据方案同步审查

> 历史说明：本文关于两个未见布局/零样本拓扑泛化的条目已被
> `plan/review/g3-6-fleet-load-amendment.md` 取代；不得继续作为当前协议依据。

## 规格符合性审查

- 通过：投稿定位已改为中科院二区现实目标，并保留一区为条件性上探。
- 通过：正式学习组统一为八个匹配训练 seed；三 seed 组被明确限制为诊断，不作显著性主张。
- 通过：外部 MARL、端到端启发式规划、规则语义、扰乱标签、NoWP、两个未见布局、
  标签盲审和 A* 运行时审计均进入必需证据链。
- 通过：CBS、在线自然语言加急、学习式充电、自适应教师加权和 Linux 迁移仍为条件性扩展。
- 通过：Constitution Chapter 8 与 `TASKS.md` 的 41 个任务 ID 双向一致。

## 质量审查

- 通过：RuleKD 和 ShuffleKD 都保持 A*KD 开启，避免把路径教师差异混入 LLM 标签比较。
- 通过：QMIX-WP 必须共享 waypoint、环境、奖励、安全层、步数和调参预算；只允许在 G4
  前按预注册规则切换到 IPPO-WP 或 VDN-WP。
- 通过：主要效用、样本效率和安全约束已区分；评估 episode 未被误当作独立训练样本。
- 通过：未见布局被限定为冻结后的 evaluation-only 零样本泛化，避免结果导向改图。
- 通过：未把任何新增实验写成已完成；G3-4 已因旧五-seed流水线失效而重新打开。

## 剩余边界

- 现有实验协议、manifest、聚合脚本、表格 schema 和图数据清单仍是旧五-seed版本，必须在
  G3-4 完成后才能正式冻结。
- 新增基线、标签对照和未见布局尚未实现；其实现与 smoke 分别由 G3-5、G3-6 和 G4-5 跟踪。
- G2-1d/e 仍未完成，继续作为 G3 的前置阻塞项。
