- 在每次完成重大改动时，进行git版本管理
- 在有不明确的细节时，向我询问来保证正确性
- 目录下的文档可以作为重要参考，但不一定要100%遵循，如有更好方法可以更改
- 虚拟环境采用py310；针对设备，分别有无独显的intel i5 huawei matebook（本人电脑） 以及 4080S的服务器 （实验室设备）
  - 尽量采用matebook做训练，除非项目训练的episode过大，CPU训练耗时长，可以考虑采用4080S
  - 每个新的训练阶段必须先在 MateBook 本地 CPU 进行 800-1000 episode 的小规模可行性测试，记录配置、运行时间、曲线和结果；通过后再决定是否扩大训练。
  - 仅当本地小规模测试可行、且大规模训练的 episode 数或预计耗时不适合 CPU 时，才允许转移到 4080S 服务器。
  - 使用 4080S 前必须在训练记录中明确服务器运行环境为原生 Linux 或 WSL，并记录 CUDA/PyTorch 版本、设备型号、命令和实验输出目录。
- 当遇到权限问题或者网络问题，告诉我，由我来解决
- 该项目需要配备有任务清单，实时记录完成的任务
