# 大规模动态仓储 LLM-MAPPO 总体方案

> 架构：LLM (优先级教师) + A\* (路径教师) → MAPPO (两级蒸馏)
> 核心范式：LAMARL 离线先验生成 + 两级蒸馏

---

## 1. 目标与边界

本项目的最终验收目标是构建一个中大规模动态物流仓储协同系统：在 `80 × 120` 的地图中，由 `10` 个 AGV 长时间协同搬运随机到达的货物批次。仓库包含 6 个拣选站、10 个充电站；运行期间允许用户通过自然语言实时调整货物优先级。

默认优先级遵循 FIFO 规则——货物入库越早优先级越高。用户可以显式覆盖（如"把 B4 调到最高优先级"），但 LLM 仅负责优先级语义转换，不参与任务-Agent 分配。执行阶段 LLM 不进入 AGV 的逐 step 动作循环；训练阶段 LLM 作为离线教师，通过蒸馏将优先级语义内化至 MAPPO 策略。

---

## 2. 设计动机

```text
训练期：
  LLM（离线）→ 优先级标签调整 + 投入度偏好生成 ──→ MSE 蒸馏 ──→ MAPPO 投入度头
  A*（在线）→ waypoint + 平滑方向偏好 ───────────→ KL 蒸馏 ──→ MAPPO 运动控制头
  损失：L = L_MAPPO + α_engage·‖e_actor - e_LLM‖² + α_path·D_KL(π_A* ‖ π_motion)

执行期：
  MAPPO 独立运行，LLM 零调用。A* 仅提供 waypoint 作为观测特征（不参与蒸馏）。
  用户输入"调高 B4 优先级"→ LLM 调整标签（1-3s）→ MAPPO 立即感知新优先级并调整投入度。
```

### 2.1 范围与验收

- 最终验收规模：`80 × 120 / 10 AGV`
- 核心创新：LLM 离线优先级蒸馏 + A\* 路径蒸馏 → MAPPO 两级自主执行
- LLM 运行时仅用于解析用户指令并调整优先级标签，不做任务分配
- 增设降级验收目标：`large/6agv` 作为银牌目标

---

## 3. 总体架构：四类组件 + 教师-学生范式

系统包含四类本质不同的组件，各自有明确的约束性质：

| 组件类型     | 实例                                        |    运行阶段    |                约束性质                 |
| ------------ | ------------------------------------------- | :------------: | :-------------------------------------: |
| **语义教师** | LLM                                         | 训练期（离线） |   软约束（MSE 蒸馏，训练后衰减至零）    |
| **路径教师** | A\*                                         | 训练期+执行期  | 软约束（KL 蒸馏）→ 执行期退化为观测特征 |
| **规则层**   | FIFO 调度 / 电量过滤 / JSON 校验 / 任务锁定 |     执行期     |  **硬约束**（不可违反，环境强制执行）   |
| **执行层**   | MAPPO 两级 Actor                            | 训练期+执行期  |  接收训练期软约束引导，执行期完全自主   |

### 3.1 架构图（训练期）

```text
                    ┌──────────────────────┐
                    │   用户自然语言指令     │
                    └──────────┬───────────┘
                               │
                               ▼
    ┌──────────────────────────────────────────────────────────┐
    │                      规则层（硬约束）                      │
    │  · FIFO 任务排序 (A>B>C，同字母按数字)                     │
    │  · 电量过滤：AGV 电量 <20% → 排除任务分配                  │
    │  · JSON 校验 + 任务锁定 + 站点容量                        │
    └──────────┬───────────────────────────┬───────────────────┘
               │ 写入 TaskQueue            │ 写入 TaskQueue
               ▼                           ▼
    ┌──────────────────────┐    ┌──────────────────────┐
    │  LLM 优先级教师（离线）│    │   A* 路径教师（在线）  │
    │ · 解析 NL → 优先级标签 │    │ · 生成 waypoint 序列   │
    │ · 生成投入度偏好 e_LLM │    │ · 输出平滑方向偏好 π_A* │
    └──────────┬───────────┘    └──────────┬───────────┘
               │  MSE 蒸馏（软约束）        │  KL 蒸馏（软约束）
               ▼                           ▼
    ┌──────────────────────────────────────────────────────────┐
    │                    MAPPO Actor（学生）                    │
    │                                                          │
    │       共享编码器 → ┌── 投入度头 e ∈ [0,1]  ← LLM 蒸馏     │
    │                   │                                      │
    │                   └── 运动控制头 π_motion ← A* 蒸馏       │
    │                                                          │
    │  训练损失：L = L_MAPPO + α_engage·MSE(e, e_LLM)          │
    │                       + α_path·D_KL(π_A* ‖ π_motion)     │
    └──────────────────────────────────────────────────────────┘
```

### 3.2 各组件职责（执行期）

| 组件       |       频率        | 职责                                                       |                       约束性质                       |
| ---------- | :---------------: | ---------------------------------------------------------- | :--------------------------------------------------: |
| **规则层** |      每事件       | FIFO 排序、电量过滤、JSON 校验——**硬约束，MAPPO 不可违反** |                     环境强制执行                     |
| **LLM**    |   仅用户输入时    | 解析自然语言 → 调整优先级标签                              | 蒸馏已完成（α_engage 已衰减至 0），LLM 仅做语义→符号 |
| **A\***    |    每任务触发     | 生成 waypoint 序列注入 MAPPO 观测                          |         **观测特征**（非命令），MAPPO 可偏离         |
| **MAPPO**  | 每 step（~100ms） | 完全自主：投入度头 + 运动头输出动作                        |              最终决策者，不受软约束限制              |

> **注意**：LLM 在训练期是投入度蒸馏的**教师**（离线生成 $e_{LLM}$ 标签，通过 MSE 损失塑造投入度头权重）。执行期 LLM 不再生成蒸馏标签——因为知识已迁移至 MAPPO 投入度头的神经网络权重中。LLM 运行时仅做优先级标签调整，MAPPO 依靠蒸馏阶段学会的权重自主响应标签变化。

### 3.3 关键约束

- **硬约束（不可违反）**：地图边界、静态障碍、电量耗尽停机、任务锁定、规则层过滤结果
- **软约束（训练期引导，执行期零约束）**：LLM 投入度蒸馏（α_engage 衰减至 0）、A\* 方向蒸馏（α_path 衰减至 0）
- LLM 不直接输出环境动作，不做任务-Agent 匹配
- MAPPO 不承担全局任务分配
- 执行期 MAPPO 独立运行，LLM 零蒸馏、零逐 step 调用

---

## 4. LLM 优先级教师

### 4.1 货物优先级标记规则

货物标签格式：`{字母}{数字}`，字母表示优先级，数字表示入库序号。

**字母不再限定为固定的 A/B/C。** 字母是动态分配的优先级标签：每个批次在创建时被赋予一个字母，不同批次可以使用相同字母表示相同优先级。系统中的字母种类数等于实际存在的优先级等级数。

| 规则                  | 说明                                                                   |
| --------------------- | ---------------------------------------------------------------------- |
| 字母分配              | 批次创建时根据预设优先级或用户指定分配字母（A 为最高，字母表顺序递降） |
| 相同字母 = 相同优先级 | 两个不同批次的货物共享同一个字母，意味着它们在调度中具有同等优先级     |
| 数字唯一性            | 每个字母下的数字序列独立编号，按入库顺序递增                           |
| 调度顺序              | 先按字母排序（A > B > C > ...），同字母内按数字 FIFO                   |

**示例**：

```text
场景: 5 个批次到达
  批次 1 (紧急, 供应商 X): 字母 A → A1, A2
  批次 2 (常规, 供应商 Y): 字母 B → B1, B2, B3
  批次 3 (紧急, 供应商 Z): 字母 A → A3, A4    ← 与批次1同优先级
  批次 4 (低优, 可延迟):   字母 D → D1, D2
  批次 5 (常规, 供应商 W): 字母 B → B4         ← 与批次2同优先级

调度顺序: A1 → A2 → A3 → A4 → B1 → B2 → B3 → B4 → D1 → D2
          └── A级 (两批次共享) ──┘   └── B级 (两批次共享) ──┘   └ D级 ┘
```

用户干预时，LLM **仅调整字母标签**，不改变数字、不做任务-Agent 分配。

### 4.2 LLM 优先级调整流程

```text
用户输入: "把 B4 调到最高优先级"

LLM 输入 Prompt:
  当前系统中使用的字母: A, B, D（共 3 个优先级等级）
  当前队列: A1, A2(waiting 45s), B1, B2, D1, B3, A3, B4(waiting 120s), D2
  用户指令: "把 B4 调到最高优先级"

  规则:
  - 当前最高优先级字母 = A（字母表最靠前）
  - 将目标货物 B4 的字母改为 A，保持数字 4 不变 → A4
  - 若 A4 已存在，原 A4 降级为 B4（字母互换）
  - 若 A4 不存在，直接改标签，不影响其他货物
  - 只改字母，不改数字，不做任务分配

LLM 输出:
  {
    "adjustments": [
      {"task": "B4", "new_label": "A4", "reason": "用户指定最高优先级"},
      {"task": "A3", "new_label": "B3", "reason": "此处无 A4 冲突，A3 不受影响"}
    ],
    "new_queue_order": "A1, A2, A4, B1, B2, B3, D1, D2"
  }
```

**降级操作**：用户说"把 A3 降到最低优先级"→ 目标字母改为当前最后一个字母（如 D），若 D3 存在则互换。

### 4.3 LLM 投入度偏好生成（训练期蒸馏数据）

LLM 在训练期为关键场景生成投入度标签 $e_{LLM} \in [0,1]$，作为 MAPPO 投入度头的 MSE 蒸馏目标。

LLM 的核心价值在于**处理需要语义理解和上下文推理的协调行为**——纯奖励学习难以精确传达、但 LLM 可以基于语义理解给出合理判断的场景：

```text
LLM 训练期输入（批量离线处理）:

【语义协调类】需要理解状态语义的决策:

场景 1: AGV_1 载货 A1，窄通道入口，对面 AGV_2 空载正接近
  → LLM: "载货 AGV 在窄通道机动性差、碰撞风险高。空载 AGV 让行代价更小。
         但 A1 是最高优先级——让 AGV_2 先过，AGV_1 短暂等待后快速通过。"
  → e_LLM = 0.25（降低投入度，等待空载 AGV 先通过窄通道）

场景 2: AGV_1 空载，窄通道入口，对面 AGV_2 载货 C2 正接近
  → LLM: "空载 AGV 让行代价低，C2 虽为低优先级但载货状态下的碰撞成本更高。"
  → e_LLM = 0.20（低投入度，主动避让载货 AGV）

场景 3: AGV_1 载货 A1，前方交叉口有 AGV_2 载货 C2
  → LLM: "A1 优先级远高于 C2，即使双方均载货，AGV_1 应优先通过。"
  → e_LLM = 0.85

【优先级判断类】需要理解优先级语义的决策:

场景 4: AGV_1 载货 A1，前方交叉口有 AGV_2 载货 C2
  → LLM: "A1 优先级高于 C2，AGV_1 应优先通过" → e_LLM = 0.85

场景 5: AGV_1 载货 C3，前方交叉口有 AGV_2 载货 A2
  → LLM: "C3 应让行 A2" → e_LLM = 0.25

【综合权衡类】多重因素的语义折中:

场景 6: AGV_1 载货 B2，电量 18%，充电站在附近
  → LLM: "电量警戒，优先充电而非继续任务" → e_LLM = 0.10

场景 7: AGV_1 载货 B2，无其他 AGV 竞争，路径畅通
  → LLM: "无竞争，正常执行" → e_LLM = 0.60
```

**LLM 负责 vs 奖励负责的边界：**

| 行为类型 | 示例 | 负责方 |
|------|------|:---:|
| 基本避碰（前方有障碍就停） | 正前方有墙 → WAIT | 奖励学习 |
| 充电时机判断 | 电量 < 0.2 → 前往充电站 | 奖励学习 |
| **窄通道载货避让空载** | 理解"载货 vs 空载"的语义差异 | **LLM 蒸馏** |
| **高优先级抢行/低优先级让行** | 理解"A1 vs C2"的语义差异 | **LLM 蒸馏** |
| **电量低时放弃任务去充电** | 理解"电量危险 > 任务完成" | **LLM 蒸馏** |

**投入度的行为语义**：

|  e 值   | 行为倾向                                       |
| :-----: | ---------------------------------------------- |
| 0.8-1.0 | 高度投入：优先通过交叉口、减少等待、不主动让行 |
| 0.4-0.7 | 正常执行：遵守规则、合理让行                   |
| 0.1-0.3 | 低投入：主动让行、选择安全路径、优先充电/维护  |

### 4.4 安全校验

LLM 优先级调整输出必须通过规则引擎：

| 校验项                              | 违反处理                                     |
| ----------------------------------- | -------------------------------------------- |
| 标签格式不合法（非大写单字母+数字） | 拒绝，保持原标签                             |
| 字母超出系统当前使用的字母表范围    | 拒绝（如系统只有 A/B/D，LLM 输出 X3 则拒绝） |
| 数字被篡改                          | 拒绝，仅允许字母变更                         |
| 调整后同一字母下出现重复数字        | 拒绝，LLM 需处理冲突（互换或赋予新数字）     |
| JSON 解析失败 / 超时                | 优先级不变，任务流继续                       |

---

## 5. A\* 路径教师

### 5.1 基础路径规划

- 在离散网格上运行标准 A\* 搜索
- 启发函数：曼哈顿距离（4 方向）或 Octile 距离（8 方向）
- 静态障碍预置为不可通行节点
- 输出：waypoint 序列 + 路径总长度

### 5.2 方向偏好生成（训练期蒸馏数据）

A\* 不仅输出 waypoint，还为每个决策步生成**平滑方向偏好**作为 MAPPO 运动控制头的 KL 蒸馏目标：

```text
A* 路径: 当前位置 P → waypoint W
A* 最优方向: RIGHT

A* 输出的 π_A*（非 one-hot，保留柔性）:
  {FORWARD: 0.82, LEFT: 0.05, RIGHT: 0.02, TOGGLE_LOAD: 0.06, NOOP: 0.05}

→ RIGHT 方向权重最高，但允许 MAPPO 在一定范围内偏离
→ 当 MAPPO 因避碰需要临时偏离时，KL 惩罚可控
```

### 5.3 多 AGV 协调

- 窄通道预约：宽度为 1 格的通道在 AGV 进入前预约时间段
- 交叉口优先级：先到达者优先
- 冲突检测：时空重叠时后规划者绕行或等待

### 5.4 训练期与执行期的不同角色

|    阶段    | A\* 角色   | 如何影响 MAPPO                                 |                 约束性质                 |
| :--------: | ---------- | ---------------------------------------------- | :--------------------------------------: |
| **训练期** | 运动教师   | 输出平滑方向偏好 π_A\* → KL 蒸馏引导运动头更新 |  **软约束**（KL 惩罚，α_path 衰减至 0）  |
| **执行期** | 特征提供者 | 输出 waypoint 序列 → 编码为观测中的 (Δx, Δy)   | **观测特征**（MAPPO 可以偏离，不会受罚） |

### 5.5 与 MAPPO 的接口

- A\* 输出 waypoint 序列 → 编码为 MAPPO 观测中的相对坐标 (Δx, Δy)
- waypoint 是**观测特征而非命令**——MAPPO 运动头自主决定动作，可以在避障时偏离 waypoint
- MAPPO 上报 `STALLED` / `BLOCKED` → A\* 重新规划并更新 waypoint
- 当 MAPPO 长期偏离 waypoint（>50 步无进展）→ 强制触发 A\* 重规划

---

## 6. MAPPO 学生：两级 Actor + 双教师蒸馏

### 6.1 观测空间

| 观测分量               | 维度  | 说明                                            |
| ---------------------- | :---: | ----------------------------------------------- |
| 局部网格               |  9×9  | 以 AGV 为中心的占用网格                         |
| 自身状态               |   4   | 位置、电量、载货状态、当前任务优先级标签编码    |
| 当前 waypoint 相对坐标 |   2   | (Δx, Δy)                                        |
| 邻近 AGV 状态          |  N×5  | 最近 3 个 AGV 的相对位置、载货优先级、速度      |
| 全局特征               |   3   | 队列中最高优先级、同区域 AGV 数量、充电站占用率 |

### 6.2 两级 Actor 架构

```text
                    观测 o_t
                       │
              ┌────────┴────────┐
              ▼                 ▼
         共享编码器 MLP [128, 64]
              │                 │
    ┌─────────┴─────┐   ┌───────┴──────────┐
    ▼               ▼   ▼                  ▼
投入度头            运动控制头
MLP [64, 1]        MLP [64, 5]
输出: e ∈ [0,1]    输出: π_motion (5维分布: FORWARD/LEFT/RIGHT/TOGGLE_LOAD/NOOP)
    │               │
    ▼               ▼
LLM 蒸馏目标       A* 蒸馏目标
MSE: ‖e - e_LLM‖²  KL: D_KL(π_A* ‖ π_motion)
```

### 6.3 训练损失函数

$$L_{total} = L_{MAPPO} + \alpha_{engage} \cdot \frac{1}{N}\sum_{i=1}^{N}(e_i - e_{LLM,i})^2 + \alpha_{path} \cdot D_{KL}(\pi_{A*} \| \pi_{motion})$$

|       系数        | 初始值 | 衰减策略              | 作用               |
| :---------------: | :----: | --------------------- | ------------------ |
| $\alpha_{engage}$ |  0.1   | 每 1000 episode × 0.9 | LLM 投入度蒸馏权重 |
|  $\alpha_{path}$  |  0.05  | 每 2000 episode × 0.9 | A\* 路径蒸馏权重   |

**衰减的意义**：训练初期 MAPPO 需要教师引导；随着策略成熟，蒸馏权重衰减，MAPPO 逐步自主化。最终执行阶段 $\alpha_{engage} = \alpha_{path} = 0$。

### 6.4 奖励设计

| 奖励项                 |             值              | 说明                                             |
| ---------------------- | :-------------------------: | ------------------------------------------------ |
| 到达 waypoint          |            +1.0             | 鼓励跟踪路径                                     |
| 完成任务               |   +5.0 × priority_weight    | 高优先级任务获得更高完成奖励                     |
| 每步惩罚               |            -0.01            | 鼓励效率                                         |
| 碰撞                   |            -2.0             | 安全约束                                         |
| **AGV 电量耗尽死亡**   |      **-20.0（全局）**      | 每死亡一个 AGV，集中式 Critic 感知到全局价值崩塌 |
| 低电量不充电（<20%）   |          -0.5/step          | 惩罚忽视充电的行为                               |
| 等待过长（>20 步连续） | -0.1/step × priority_weight | 高优先级任务等待惩罚更重                         |

其中 `priority_weight` 由当前载货任务的字母标签动态决定：

$$priority\_weight(letter) = 0.5 + 1.5 \times \frac{N - rank(letter)}{N - 1}$$

其中 $N$ = 系统中当前使用的字母总数，$rank(letter)$ = 该字母在字母表中的排名（A=0, B=1, ...）。

例如：系统使用 3 个字母 {A, B, D} → A=2.0, B=1.25, D=0.5。系统使用 5 个字母 → A=2.0, 中间字母≈1.1-1.5, 最末字母=0.5。

**电量协调的学习机制**：集中式 Critic 在训练期观察到所有 AGV 的电量状态。当高电量 AGV 抢占了低电量 AGV 的充电机会导致后者死亡时，Critic 感知到 -20.0 的价值崩塌 → 通过 Advantage 传递到 Actor 策略 → AGV 学会"电量 65% 时在充电站遇到 12% 电量的同伴，降低投入度，让行"。这是 CTDE 框架的天然优势——不需要 LLM 或 A\* 介入，MAPPO 通过奖励信号和全局 Critic 自主学会电量协调。

### 6.5 动作空间

采用 RWARE 朝向制动作系统：

- **5 个离散动作**：{FORWARD, LEFT, RIGHT, TOGGLE_LOAD, NOOP}
- 充电动作通过环境在充电站位置触发（Agent 在充电站执行 NOOP 时自动充电）
- FORWARD 基于 Agent 当前朝向，LEFT/RIGHT 为 90° 原地旋转
- TOGGLE_LOAD：在货架装载/卸载点装卸货物
- 投入度 $e$ 不直接输出动作，而是 **调制运动头的动作选择**：

$$a_{final} = \arg\max_a \left[ \pi_{motion}(a|o) \cdot (1 + e \cdot \mathbf{1}[a = \text{FORWARD}]) \right]$$

当 $e$ 高时，FORWARD 动作的权重被相对提升——AGV 更倾向于持续前进而非停留或旋转。

### 6.6 Critic 网络

- 集中式 Critic：注意力池化 [256, 128] + 价值头
- 聚合所有 AGV 的编码特征 + 全局统计量
- 扩展到 80×120 时升级为多尺度地图编码

### 6.7 异常上报

| 事件              | 条件                               | 处理             |
| ----------------- | ---------------------------------- | ---------------- |
| `STALLED`         | 连续 30 步无法缩短到 waypoint 距离 | A\* 重新规划     |
| `BLOCKED`         | 所有合法动作被占用                 | A\* + 预约表调整 |
| `REPLAN_REQUIRED` | 静态障碍变化或路径失效             | A\* 全局重规划   |

---

## 7. 执行期 LLM 调用

LLM 在执行期**仅做优先级标签调整**，且仅在用户下达自然语言指令时触发。所有常规调度逻辑由 MAPPO + A\* 自主完成。

| 场景                           | 是否调用 LLM |          延迟容忍           |
| ------------------------------ | :----------: | :-------------------------: |
| 用户输入"把 B4 调到最高优先级" |  是（1-3s）  | 可接受（不影响 MAPPO 运行） |
| 新批次到达（无用户干预）       |      否      |              —              |
| AGV 动作执行                   |      否      |              —              |
| A\* 路径规划                   |      否      |              —              |
| 地图或设备状态变化             |      否      |              —              |

**训练期 LLM 使用**：
- 离线批量生成投入度偏好标签（$e_{LLM}$）和训练场景优先级调整样例
- 不参与在线训练循环——标签预先生成，训练时直接读取
- 总成本可控：预计 500-1000 条场景标注，约 ¥2-5

---

## 8. 仿真环境规格

仿真环境基于 RWARE（Multi-Robot Warehouse）进行魔改实现。本节描述魔改后环境的完整规格。

### 8.1 基础框架

- **基础环境**：RWARE（Gymnasium 接口，`reset(seed)` / `step(actions)` / `render()`）
- **Agent 移动方式**：采用rware的移动方式
- **货架交互**：Agent 需站在货架指定位置执行 TOGGLE_LOAD 完成装货/卸货
- **货架语义**：装货后 Agent 携带货架移动；卸货后货架留在原地，Agent 空载

### 8.2 拣选站与充电站

- **拣选站**：货物交付的目标位置。Agent 将载货的货架搬运至拣选站后卸货。拣选站完成一次拣选任务需消耗 **3 个 steps**（Agent 在此期间处于拣选锁定状态，持续耗电 -0.0002/step，不可执行其他动作）
- **充电站**：数量 = AGV 数量（N 个 AGV 对应 N 个充电站）。Agent 在充电站位置执行相应动作即可充电，充电速率由环境内部控制

### 8.3 碰撞机制

当两个 AGV 处于相邻格子，且任意一个 AGV 朝向另一 AGV 所在格子方向执行 FORWARD 时，记为一次碰撞：

```text
碰撞判定条件：
  1. AGV_A 与 AGV_B 处于相邻格子
  2. AGV_A 朝 AGV_B 所在方向执行 FORWARD
  → 碰撞发生，对碰撞发起方施加惩罚

被碰撞方（静止在格子中）不受惩罚。
双方同时向对方格子移动 → 两次独立碰撞 → 双方均受惩罚。
```

碰撞惩罚通过环境奖励机制实现（见 6.4 节奖励设计）。

### 8.4 货物入库逻辑

- **入库周期**：每隔固定的 s 个 steps，触发一次入库事件
- **入库数量**：每次入库 n 个货物，n ∈ [n_min, n_max]，由随机过程在区间内均匀采样
- **批次定义**：同一入库事件到达的 n 个货物称为一个**批次**
- **优先级分配**：
  - 同一批次内的所有货物具有相同的优先级
  - 默认优先级按批次入库时间排序：越早入库的批次优先级越高
  - 优先级通过与排序位置对应的字母标签体现（A 为最早入库，字母表顺序递降）
- **奖励关联**：搬运优先级越高的货物的 Agent 获得越高的完成奖励（见 6.4 节 priority_weight）

### 8.5 AGV 电量设计

总电量为 1.0（满电），每 step 各动作耗电如下：

```text
AGV 状态 / 动作                     每 step 电量变化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━
空载成功前进 FORWARD                   -0.001
空载成功左转 LEFT                      -0.001
空载成功右转 RIGHT                     -0.001
空载成功装货 TOGGLE_LOAD               -0.002
载货成功前进                           -0.002
载货成功左转 / 右转                    -0.002
载货成功装货相关动作                   -0.002
NOOP                                  -0.0002
动作非法或执行失败                    -0.0002
AGV 正在拣选锁定状态                  -0.0002
成功充电                              不耗电
```

电量 < 0.2 为低电量警戒区，持续低电量不充电将受到奖励惩罚（见 6.4 节）。电量降至 0 时 AGV 死亡。

---

## 9. 分阶段实施路线与 Go/No-Go

### Phase 1：仿真环境集成与接口冻结

**仿真基础**：基于 RWARE 魔改环境，已包含 8.2-8.5 节所述全部规格。

**规模**：medium / 3 AGV / 2 拣选站 / 3 充电站（N AGV = N 充电站）

**交付物**：
- 在魔改后的 RWARE 环境中确认/集成以下规格（见第 8 节）：
  - 货物批次入库：每 s steps 随机入库 n ∈ [n_min, n_max] 个货物，同批次同优先级
  - 电池消耗模型（空载前进 -0.001 / 载货前进 -0.002 / NOOP -0.0002 / 充电不耗电）
  - 碰撞机制：相邻格子 FORWARD 进入占用格子 → 计碰撞 + 惩罚
- `PriorityAdjustment`、`EngagementLabel` 数据结构冻结
- A\* 路径规划器 + 方向偏好生成器（适配朝向制动作空间）
- 规则层实现：FIFO 调度器 + 电量过滤（AGV < 0.2 排除任务分配）+ JSON 校验

**可视化**：仿照 RWARE 原项目的 `human_play` 脚本，编写 `demo.py`：

| 功能 | 说明 |
|------|------|
| 环境渲染 | 实时显示网格地图、AGV 位置与朝向、货架状态、拣选站/充电站位置 |
| 信息叠加 | 在 AGV 上方或侧边显示当前电量、载货标签、投入度值、执行任务 |
| 键盘控制 | 方向键控制选中 AGV 的 FORWARD/LEFT/RIGHT，空格 TOGGLE_LOAD，回车步进 |
| AGV 切换 | Tab 键切换控制不同 AGV |
| 自动模式 | 按 R 键切换为规则驱动自动运行（FIFO + A\* 路径），方便快速验证环境逻辑 |
| 统计面板 | 侧边栏或终端输出：当前 step、任务完成数、碰撞次数、各 AGV 电量、批次队列 |

`demo.py` 在后续每个 Phase 中持续维护，确保算法改进的效果可直观验证、问题可快速定位。

**Go/No-Go**：RWARE 环境在集成扩展后 ≥100 episode 无掉帧，A\* + 方向偏好通过单元测试

**对应论文出图**：图 12（仿真实验环境）



---

### Phase 2：MAPPO 局部执行验证 ⚠️ 核心风险关卡

**规模**：medium / 3 AGV / 单优先级（均为 B 级） / oracle 路径 / 3 充电站

**目标**：在 oracle 条件下验证 MAPPO 基础运动控制能力收敛，含充电时机选择和充电站协作。

**训练**：先 1 AGV（纯 waypoint 跟踪 + 充电时机学习）→ 3 AGV（加入避碰 + 充电站让行协调）

**通过标准**：

| 指标                   |            阈值             |
| ---------------------- | :-------------------------: |
| 任务完成率             |            ≥ 95%            |
| 每 episode 碰撞次数    |             ≤ 2             |
| 死锁率                 |        ≤ 5% episode         |
| 训练收敛               | ≤ 5000 episode 内 loss 稳定 |
| 成功率方差（10 seeds） |            ≤ 10%            |

**Go/No-Go**：
- ✅ 通过 → 进入 Phase 3a
- ❌ 未通过 → 修复 reward/网络/观测，重新验证，不得跳过

**对应论文出图**：图 3（MARL 架构）、图 11（训练验证流程）、图 13（基础对比，MAPPO vs 规则方法）



---

### Phase 3a：两级 Actor + 多优先级（无蒸馏）

**规模**：medium / 3 AGV / 多优先级混合批次

**变更（唯一变量：架构 + 环境）**：
- MAPPO 单头 → 两级 Actor（投入度头 + 运动控制头）
- 单优先级 → 多优先级（动态 priority_weight 奖励）
- **不加蒸馏**：投入度头使用规则标签（高优先级=0.8, 中=0.5, 低=0.3）

**规则投入度的覆盖范围**：规则标签仅提供基于优先级的粗粒度投入度，MAPPO 在此阶段通过奖励函数自主学会基本避碰、充电时机等**生存本能行为**。需要语义理解的协调行为（窄通道载货/空载避让、多因素权衡）留待 Phase 4a 通过 LLM 蒸馏实现。
- **不加 A\* KL 项**：训练仍用纯 MAPPO loss

**Go/No-Go**：两级 Actor 在多优先级下任务完成率 ≥ Phase 2（架构升级不退化），且高优先级任务平均完成时间 < 低优先级

**对应论文出图**：图 5（状态空间）、图 6（动作空间）

---

### Phase 3b：A\* 路径蒸馏

**规模**：medium / 3 AGV / 多优先级混合批次（与 3a 完全相同）

**变更（唯一变量：A\* 蒸馏）**：
- A\* KL 蒸馏接入训练 loss：$L = L_{MAPPO} + \alpha_{path} \cdot D_{KL}(\pi_{A*} \| \pi_{motion})$
- 其他不变（架构、环境、奖励与 3a 相同）

**消融实验**：

| 条件 | 含义 |
|------|------|
| 两级 MAPPO（无蒸馏）| Phase 3a 产物 |
| + A\* 路径蒸馏 | Phase 3b 完整 |

**Go/No-Go**：A\* 蒸馏后任务完成率不下降，若下降则调整 $\alpha_{path}$

**对应论文出图**：图 13（MAPPO vs 规则基线对比）、图 15（消融：A\* 蒸馏 on/off）



---

### Phase 4a：LLM 优先级蒸馏（保持小规模）

**规模**：medium / 3 AGV / 模拟用户优先级调整（与 Phase 3b 规模相同）

**变更（唯一变量：LLM 蒸馏）**：
- LLM 离线生成投入度标签 $e_{LLM}$ → MSE 蒸馏接入训练 loss
- LLM 运行时解析用户 NL → 优先级标签调整
- 规模与 Agent 数量与 Phase 3b 完全相同

**LLM 蒸馏的核心贡献——语义协调行为**：Phase 3b 的规则投入度只能提供粗粒度优先级响应，LLM 蒸馏注入的是需要**语义理解和上下文推理**的协调能力（详见 4.3 节）：窄通道载货/空载避让、高/低优先级抢行与让行、电量危险时放弃任务等多因素综合权衡。

**LLM 配置**：
- 主力模型：DeepSeek-V4 API
- 角色一（运行时）：用户 NL 指令 → 优先级标签调整
- 角色二（训练前）：批量生成投入度偏好标签 $e_{LLM}$

**蒸馏流程**：
1. 训练前：LLM 离线处理 300-500 个关键场景 → 生成 $\{o_i, e_{LLM,i}\}$ 标签集
2. 训练期：$L_{total} = L_{MAPPO} + \alpha_{engage} \cdot \text{MSE}(e, e_{LLM}) + \alpha_{path} \cdot D_{KL}(\pi_{A*} \| \pi_{motion})$
3. 训练后期：$\alpha_{engage}$ 衰减 → MAPPO 投入度判断自主化

**消融实验**：

| 条件 | 含义 |
|------|------|
| 两级 MAPPO（规则投入度，无 LLM）| Phase 3b 产物 |
| + LLM 优先级标签调整（运行时）| 仅标签调整，不蒸馏 |
| + LLM 投入度蒸馏（完整方案）| 标签调整 + 投入度蒸馏 |

**通过标准**：LLM 蒸馏方案在 ≥2 项核心指标上显著优于规则投入度（p < 0.05, 10 seeds）

| 指标 | 目标 |
|------|:---:|
| 高优先级响应时间改善 | ≥ -25% |
| 用户优先级调整后的适应延迟 | ≤ 10s |
| 任务饥饿率 | ≤ 3% |
| LLM 标签调整成功率 | ≥ 98% |

**Go/No-Go**：至少 2 项达标 → 进入 Phase 4b

**对应论文出图**：图 2（框架总览）、图 7（LLM 状态压缩与蒸馏）、图 8（LLM-MAPPO 协同决策）、图 9（动态重调度闭环）、图 13（完整对比，含 LLM 增强）、图 15（消融：LLM 蒸馏 on/off）

---

### Phase 4b：规模扩展（保持算法不变）

**规模**：large / 6 AGV / 6 拣选站 / 6 充电站

**变更（唯一变量：规模）**：
- Phase 4a 的完整算法直接迁移至 large 规模
- Critic 网络调整以适配更大观测空间
- 仅做规模对比，不改算法

**消融实验**：

| 条件 | 含义 |
|------|------|
| Phase 4a 产物（medium/3AGV）| 小规模基准 |
| Phase 4b（large/6AGV）| 规模扩展 |

**Go/No-Go**：large 规模下性能不退化超过 10%

**对应论文出图**：图 14（不同规模泛化对比）、图 16（任务-Gantt 图）、图 17（轨迹与奖励可视化）



---

### Phase 5：目标规模扩展

**规模**：80 × 120 / 10 AGV / 6 拣选站 / 10 充电站（N AGV = N 充电站）

**关键调整**：
- Critic 升级为多尺度地图编码
- A\* 按区域分区降低搜索开销
- LLM 投入度标签扩展至 10 AGV 场景

**验收**：完整指标评估（见第 9 节），执行期零 LLM 蒸馏调用

**降级目标**：large / 6 AGV 的 LLM 增强系统作为银牌交付

**对应论文出图**：图 14（不同规模泛化对比）、图 16（任务-Gantt 图）、图 17（轨迹与奖励可视化）



---

### Phase 6（可选）：运行时 LLM 异常处理

- 用户自然语言异常描述 → LLM 解析为优先级调整或区域约束
- 异常知识库（ALAS 风格）
- 完整消融实验
- 不阻塞最终验收

**对应论文出图**：图 9（异常触发重调度）

---

## 9. 验收指标

### 9.1 核心性能指标

| 指标                 | 说明                             |
| -------------------- | -------------------------------- |
| 任务吞吐             | 单位时间完成任务数               |
| 平均等待时间         | 任务从到达到完成的平均时间       |
| P95 等待时间         | 95% 任务的最长等待时间           |
| 高优先级响应时间     | A 级任务的平均完成时间 vs B/C 级 |
| 用户优先级的适应延迟 | 用户指令下达到系统行为调整的时间 |
| 任务饥饿率           | 等待超过 300s 的任务比例         |
| 站点负载均衡         | 各拣选站任务量的标准差           |
| 能耗                 | 单位任务平均能耗                 |
| AGV 阻塞/死锁率      | 需要外部干预的比例               |
| 路径重规划次数       | 每 episode A\* 重规划触发次数    |

### 9.2 蒸馏专项指标

| 指标                               | 说明                          |
| ---------------------------------- | ----------------------------- |
| $e_{actor}$ 与 $e_{LLM}$ 的 MSE    | 投入度蒸馏质量（训练期）      |
| $D_{KL}(\pi_{A*} \| \pi_{motion})$ | 路径蒸馏质量（训练期）        |
| 蒸馏衰减后的性能保持率             | $\alpha \to 0$ 后策略是否退化 |
| 两级 vs 单级 Actor 的收敛速度差    | 架构消融                      |

### 9.3 LLM 专项指标

| 指标                       | 说明                    |
| -------------------------- | ----------------------- |
| 优先级标签调整成功率       | 格式正确 + 通过规则校验 |
| 标签调整延迟（含 API RTT） | 用户输入到队列更新      |
| 训练期投入度标签生成量     | 标注场景数              |
| 训练期 LLM API 总成本      | 一次性成本              |

### 9.4 实验规范

- 所有对照实验使用相同地图实例、动态批次流、用户优先级事件和随机种子
- 执行阶段验证逐 step 决策循环中无 LLM 调用
- 蒸馏消融：关掉 $\alpha_{engage}$ 和 $\alpha_{path}$ 分别量化两个教师的贡献
- 每个实验条件 ≥10 个随机种子，报告均值和标准差

---

## 10. 论文出图与任务清单

### 10.1 Phase → 论文图映射总表

| Phase | 论文图号 | 图题 | 实验/任务内容 |
|:---:|:---:|------|------|
| 1 | 图 12 | 仿真实验环境 | RWARE 环境截图、布局说明 |
| 2 | 图 3 | MARL 整体架构 | MAPPO 训练流程 |
| 2 | 图 11 | 训练与验证流程 | 训练曲线（reward/loss/episode）|
| 2 | 图 13 | 基础对比 | MAPPO vs FIFO vs Nearest-First |
| 3a | 图 5 | 状态空间建模 | 观测向量设计与编码 |
| 3a | 图 6 | 动作空间与调度规则 | 5 动作 + 投入度调制 |
| 3b | 图 13 | 核心对比（A\* 蒸馏）| 规则 vs MAPPO vs A\* 蒸馏 MAPPO |
| 3b | 图 15 | 消融（A\* 蒸馏）| A\* 蒸馏 on/off 对比 |
| 4a | 图 2 | 框架总览 | LLM-Augmented MAPPO 完整架构 |
| 4a | 图 7 | LLM 状态压缩与蒸馏 | 投入度标签生成流程 |
| 4a | 图 8 | LLM-MAPPO 协同决策 | 两级蒸馏机制 |
| 4a | 图 9 | 动态重调度闭环 | 用户干预 → 优先级调整 → 行为变化 |
| 4a | 图 13 | 完整对比 | 规则 vs MAPPO vs LLM-Augmented |
| 4a | 图 15 | 消融（LLM 蒸馏）| LLM 蒸馏 on/off、故障注入 |
| 4b | 图 14 | 规模泛化 | medium → large 对比 |
| 5 | 图 14 | 规模泛化（补充）| 80×120 数据 |
| 5 | 图 16 | 任务-AGV 甘特图 | 各 AGV 任务执行时序 |
| 5 | 图 17 | 轨迹与奖励可视化 | 路径优化、奖励提升曲线 |
| 6 | 图 9 | 动态重调度 | 异常事件触发 LLM 重调度 |

### 10.2 论文示意图（不依赖实验）

| 论文图号 | 图题                       | 内容要求                                      |
| :------: | -------------------------- | --------------------------------------------- |
|   图 1   | 多机器人协同任务问题示意图 | 仓储场景：AGV、货架、拣选站、充电站、批次入库 |
|   图 4   | 多机器人通信与信息共享结构 | CTDE 框架下的观测共享、集中式 Critic          |

---

*方案版本：当前工作副本 | 最后更新：2026-07-30*
