# LLM-MAPPO 项目 Constitution

> **文档性质**：本文件是项目后期研发、正式实验与论文证据建设的最高层项目约束。
> 它不替代具体配置、实验协议或论文正文；当旧方案文档与当前代码、冻结配置或本
> Constitution 冲突时，应先查明原因并显式修订，不能默默沿用旧结论。
>
> **版本**：v0.2（二区现实投稿目标实验增强版）
>
> **日期**：2026-08-20
>
> **维护语言**：中文；正式论文语言为英文。
>
> **投稿定位**：以中科院二区英文方法论文作为现实目标；一区仅作为结果和创新性均
> 明显超出预期时的上探方向，不以无限扩展实验范围为代价。

## 1. Mission

本项目面向多智能体路径规划与调度问题，研究一种由异构教师共同监督的多智能体
强化学习方法。方法以 MAPPO 为动作策略，以 A* 教师提供路径偏好，以离线 LLM
教师提供高层协作语义，通过多教师知识蒸馏同时学习低层路径行为与高层任务语义。

项目的核心价值不在于构建特定仓库应用，而在于回答以下方法问题：不同知识形态的
教师能否向同一多智能体策略提供互补监督，并在保持执行期零在线 LLM 调用的同时，
改善策略的任务效率、安全性、样本效率与跨场景泛化能力。

动态物流仓库是当前主要验证环境。它提供动态任务到达、任务优先级、多智能体冲突、
窄通道、电量约束和充电拥堵等可控挑战，但论文主张必须落在方法本身，而不是依赖
单一仓库配置的工程效果。

## 2. 科学问题与研究假设

### 2.1 核心科学问题

- **RQ1：路径教师贡献。** A* 路径偏好蒸馏能否提高 MAPPO 的收敛效率、任务完成
  效率和安全性？
- **RQ2：语义教师贡献。** 离线 LLM 双语义标签能否改善任务投入、局部让行、优先级
  响应和复杂场景下的协调行为？
- **RQ3：教师互补性。** A* 路径教师与离线 LLM 语义教师联合使用时，是否产生超过
  任一单教师方法的互补收益？
- **RQ4：竞争力、效率与泛化。** 双教师方法相对外部 MARL 和端到端启发式规划基线
  是否具有竞争力，其收益能否在未见随机种子、未见仓库布局、不同任务强度、不同
  AGV 数量和电量/拥堵条件下保持，并具有可接受的训练与执行成本？
- **RQ5：LLM 语义监督的必要性与质量。** LLM 标签相对同结构的规则语义标签和扰乱
  标签是否提供有效的状态相关监督，不同 LLM 的标签质量、稳定性、成本和推理时延
  是否会显著影响语义监督的可靠性？

### 2.2 核心研究假设

- 路径蒸馏主要改善低层动作选择、路径效率和冲突规避。
- 语义蒸馏主要改善任务优先级响应、任务持续投入与局部通行倾向。
- 两类教师具有不同的知识来源和监督目标，其组合应体现可测量的互补性。
- 离线标签缓存可以把 LLM 成本限制在训练准备阶段，使训练、评估和部署保持零在线
  LLM 调用。

以上均为待实验检验的假设，不得在获得多种子真实结果前写成既定结论。

## 3. 科学主张与非主张

### 3.1 当前允许主张

- 提出由 A* 路径教师和离线 LLM 双语义教师共同监督 MAPPO 的多教师蒸馏框架。
- LLM 仅生成边界明确、可审计的离线语义标签；训练和执行不调用在线 LLM。
- 规则层继续负责硬安全约束和任务标签合法性，MAPPO 负责实际动作决策。
- 当前方法可以在统一动态多智能体环境中通过消融、泛化和效率实验进行验证。

### 3.2 当前禁止主张

- 不得声称当前策略在执行期已经摆脱 A*。现有实现仍通过 A* 生成 waypoint 观测。
- 不得声称 800 episodes 训练完成即代表方法有效或项目验收通过。
- 不得用单个 seed、最佳 checkpoint 或训练分布指标证明普遍改进。
- 不得把累计 reward 等同于完成任务数、完成率或实际吞吐量。
- 不得声称 LLM 直接分配 AGV、输出逐步动作、替代规则层或替代 A*。
- 不得把不同地图、AGV 数量、任务流或训练预算下的旧 Phase 2/3/4 结果直接作为
  公平方法对照。

## 4. 方法边界与冻结原则

### 4.1 当前主方法

主方法由以下部分组成：

1. **MAPPO 策略**：共享 Actor、集中式 Critic，输出 AGV 离散动作。
2. **A* 路径教师**：提供 waypoint 特征和时空预约路径偏好；路径偏好通过 KL 项蒸馏。
3. **离线 LLM 语义教师**：提供 `task_commitment` 与
   `local_assertiveness` 两维标签，通过缓存数据和近邻检索监督语义分支。
4. **规则与安全层**：负责任务队列、标签合法性、硬安全约束、充电安全目标和状态校验。

### 4.2 正式冻结原则

- 正式实验开始后，不得在不同实验组之间改变地图、任务流、AGV 数量、观测定义、
  动作空间、奖励函数、训练预算、评估 seed 或 checkpoint 选择规则。
- 任何影响实验语义的修复必须先停止正式批量实验，形成变更记录并判断是否需要重跑。
- 800-episode 当前训练属于可行性与流水线验证。只有在检查点加载、独立评估、回放、
  数值稳定性和安全指标检查完成后，才能用于决定正式配置。
- 一个外部 MARL 基线、一个端到端启发式规划基线、规则语义教师对照、扰乱标签负对照、
  `MAPPO-NoWP` 诊断和两个未见布局属于二区目标的必需证据，不再视为可删除扩展。
- 完全移除执行期 A*、以 CBS 替换 A*、新增自适应教师加权、在线自然语言加急、学习式
  充电和 Linux 迁移仍属于条件性扩展，不得阻塞上述必需证据链。

### 4.4 条件性扩展：执行期自然语言加急与优先级重调

项目已有标签级优先级调整的底层原型：LLM provider 可以把用户指令解析为
`PriorityAdjustment`，规则层对所有修改进行原子校验，并刷新任务队列。然而，当前尚未
形成接入训练、评估或部署执行循环的用户输入入口，因此不能宣称已支持完整的执行期
自然语言加急功能。

如核心论文证据链完成且时间允许，可实现受控的事件触发式接口，例如将“交换 A4 与
B4 的优先级”转换为合法的标签调整。该扩展必须满足：

- LLM 只解释用户的任务优先级意图，不分配 AGV、不生成路径或逐步动作；
- 规则层只接受活动任务、既有优先级字母和保留数字后缀的调整，并原子拒绝重复标签、
  非法标签及部分成功；
- 已分配任务是否允许换级、换级后是否抢占或仅影响后续调度，必须先定义并冻结；
- 记录原始指令、结构化调整、模型、时延、失败原因、调整前后队列及任务完成指标；
- 提供无在线 LLM 的确定性规则/结构化命令降级入口，避免 API 故障破坏执行安全；
- 该功能属于部署交互扩展。启用时必须单独报告在线 LLM 调用次数、时延和成本，不能
  沿用主方法“执行期零在线 LLM 调用”的表述。

### 4.3 A* 因子的实验定义必须先冻结

“是否使用 A*”存在两个不同层次：

- 是否使用执行期 A* waypoint 观测；
- 是否使用训练期 A* KL 路径蒸馏。

正式实验前必须明确区分二者。推荐核心 `2×2` 因子实验在各组保留相同 waypoint
观测，只切换 A* KL 蒸馏和离线 LLM 语义蒸馏，从而隔离两类蒸馏信号的贡献。此时
各组应准确命名为 `MAPPO-WP`、`MAPPO-WP+A*KD`、`MAPPO-WP+LLMKD` 和
`MAPPO-WP+A*KD+LLMKD`，不能把 `MAPPO-WP` 简写成完全不使用 A* 的纯 MAPPO。
四组必须使用相同的双语义网络宽度。无 LLMKD 组保留语义分支参数，但 motion head
接收固定零语义向量且语义蒸馏损失为零，避免未训练随机语义特征污染动作比较。

必须增加 `MAPPO-NoWP` 诊断组，用于测量执行期 waypoint 的独立价值。该组不改变
核心 `2×2` 的因果解释，不纳入主要显著性结论；其训练 seed 和预算必须在 G3 冻结，
用于支撑执行期 A* 依赖及方法边界的讨论。

#### 4.3.1 G2-1 时空预约最小修复方案

当前优先级时空 A* 存在三个耦合风险：`ReservationTable.reserve()` 把路径终点重复预约到
整个 horizon；时空搜索没有保持方向不变的真实 `NOOP` 等待后继，只能用左右旋转消耗
时间；未到达目标的最佳部分路径没有与完整路径明确区分。这会把短暂终点占用放大为
伪无解，并在逐步重规划时形成旋转活锁。

G2-1 只允许实施以下最小修复，不在此阶段引入 CBS、全局任务重分配或新的学习模块：

1. 时空搜索内部返回包含 `timed_positions`、首动作、`reached_goal` 和失败原因的结果，
   对外仍只提供当前一步的平滑动作偏好；部分路径必须标记为需要重规划，不能记为成功到达；
2. 增加显式 `NOOP` 等待后继，并保留转向、等待和前进的真实时间展开，避免把等待编码为
   左右旋转；
3. 将终点占用改为可配置的有界保留，正式默认 `terminal_hold_steps=2`。成功路径只预约
   实际时空轨迹和终点后的两个 step，不再占满整个 horizon；
4. 按物理语义处理静止 AGV：死亡/不可移动实体继续预约到 horizon，拾取锁按剩余锁定
   step 预约，切换装载和临时无路回退只预约当前将执行的一个 step；
5. 保持滚动时域原则：每个真实环境 step 只执行规划的首动作，下一 step 根据真实占用重新
   规划。顶点冲突、对向边交换和动作 mask 仍为硬约束，不以降低安全性换取可达性。

必须新增 `reached_goal`、部分路径、终点预约冲突、静态可达但时空未达、显式等待、重规划、
旋转活锁、状态死锁、扩展节点和规划耗时计数。诊断中“静态 A* 可达而时空 A* 未到目标”
定义为预约相关伪无解，不能与地图拓扑无路混为一类。

验证分两层：短时确定性单元/场景测试由代码协作方执行；训练、`10 seeds × 20 episodes`
等长时间运行由项目负责人执行。长时配对诊断使用校准 seed `300–309`，不得占用正式评估
seed `200–209`。修复版本相对 legacy horizon 版本必须满足：受控共享终点场景伪无解和
旋转等待为零；预约顶点/边冲突为零；任务完成率下降不超过 1 个百分点；碰撞、死锁和
能量死亡不增加；规划耗时 p95 不超过 legacy 的 1.20 倍。未通过时保留 legacy 实现并把
预约风险列为论文限制，不进入 G3 冻结。

### 4.5 条件性扩展：学习式充电决策与应急安全盾

核心实验继续把低电量目标切换视为规则层安全机制，不声称 MAPPO 自主决定充电时机。
核心证据链完成后，可将正常电量区间内的“继续任务/前往充电”建模为策略高层意图，
同时保留极低电量（建议从 `0.10` 开始 pilot）的强制充电安全盾。该扩展必须把充电
意图选择与充电路径执行分开评估，并与固定阈值规则基线使用相同能源压力、seed 和
环境步数。若启用，还必须重新定义教师监督边界、观测、奖励、任务中断/恢复协议和
消融组，不能直接并入已冻结的核心 `2×2` 实验。

## 5. Tech Stack

### 5.1 软件栈

- Python 3.10，Conda 环境 `py310`；
- Gymnasium 与项目内 RWARE 动态仓储环境；
- PyTorch 实现 MAPPO、双语义 Actor 和注意力 Critic；
- NumPy、SciPy（含 KD-tree）、NetworkX、PyYAML；
- TensorBoard、CSV、JSON/JSONL 用于监控、指标和离线标签；
- pytest 与 Flake8 用于回归测试和静态检查；
- Matplotlib/Pillow/项目可视化入口用于论文图表和确定性回放；
- Git 用于重大改动、配置冻结和实验版本追踪。

### 5.2 计算环境

- 本地 Intel i5、无独显 Huawei MateBook：代码开发、单元测试、静态检查和小规模
  smoke test；
- NVIDIA RTX A6000 服务器：多种子正式训练和大规模评估；
- 原生 Linux 迁移仅作为性能优化备选，不属于主线强制任务；若实际迁移，必须记录
  操作系统、CUDA、驱动、PyTorch、命令、配置、代码提交和产物目录。

### 5.3 教师模型

- 当前正式离线标签：DeepSeek-V4-Flash；
- 教师质量对比候选：DeepSeek-V4-Pro、GPT-5.6 Terra
  (`gpt-5.6-terra`) 和 GPT-5.6 Luna (`gpt-5.6-luna`)；
- 教师比较必须固定场景、提示词、输出 schema、采样参数和审计规则；
- API 密钥不得写入代码、配置、日志、论文材料或 Git 历史。

## 6. 正式实验宪章

### 6.1 主实验环境

- 地图：扩展后的 medium 仓库，约 `24×20`；
- AGV 数量：5；
- 动态任务批次：`[4, 8]`；
- 批次间隔：40 steps；
- 每回合任务完成目标：`N=50`；
- 最大回合长度：1000 steps；
- 核心能耗/充电滞回：`battery_cost_scale=1.10`、进入阈值 `0.30`、释放阈值 `0.80`；
- 初始优先级：A/B 批次；
- 充电站：当前外环 8 站点机制；
- 动作、观测、奖励、规则层和安全约束在核心实验间保持一致。

### 6.2 核心消融与必需比较

在 A* 因子定义冻结后，至少完成以下四组：

| 组别 | A* KL 路径蒸馏 | 离线 LLM 语义蒸馏 | 角色 |
|---|---:|---:|---|
| MAPPO-WP | 否 | 否 | 统一 waypoint 下的基础组 |
| MAPPO-WP+A*KD | 是 | 否 | 路径教师单独贡献 |
| MAPPO-WP+LLMKD | 否 | 是 | 语义教师单独贡献 |
| MAPPO-WP+A*KD+LLMKD | 是 | 是 | 完整主方法 |

除核心 `2×2` 外，二区现实目标至少需要以下比较：

| 组别 | 预算 | 作用与公平边界 |
|---|---:|---|
| QMIX-WP | 8 个训练 seed | 外部 MARL 基线；共享 waypoint、环境、奖励、安全层、训练步数和调参预算 |
| MAPPO-WP+A*KD+RuleKD | 8 个训练 seed | 保持 A*KD 开启，以冻结规则生成同维度语义标签，检验 LLM 监督是否优于廉价规则监督 |
| MAPPO-WP+A*KD+ShuffleKD | 3 个诊断 seed | 保持 A*KD 开启及标签边际分布，但打乱状态—标签对应，仅作负对照 |
| MAPPO-NoWP | 3 个诊断 seed | 关闭两类 KD 并移除 waypoint，与 MAPPO-WP 比较以量化执行期 A* 依赖 |
| Heuristic-Dispatcher+A* | 无训练 | 规则优先级调度与优先级时空 A* 组成的端到端非学习基线 |

RuleKD 必须从同一冻结的 400 状态缓存以版本化确定性映射生成，ShuffleKD 必须使用固定 seed
对完整二元语义标签实施无固定点置换；二者都不得调用在线模型。NoWP 保持 actor 观测宽度，
但将 waypoint 特征槽固定为零，并同时关闭 waypoint shaping reward 与 A*KL。启发式基线
复用同一动态任务分派、动作 mask 与规则安全层。上述实现与数据路径必须写入唯一运行清单。

QMIX-WP 如在 G4 前被证实无法在相同观测、动作和安全边界下形成公平实现，必须在 G3
协议增补中预注册 `IPPO-WP` 或 `VDN-WP` 之一作为替代；不得在观察正式结果后选择更弱
或更有利的基线，也不得完全省略外部 MARL 比较。传统 A* 只作为路径规划参考，不得冒充
包含动态任务分配、优先级和充电语义的端到端基线。CBS 保持条件性扩展。

### 6.3 训练预算与 seed

- 核心四组使用相同的最大环境交互步数，而不是仅统一 episode 数；
- 核心四组、QMIX-WP 和 MAPPO-WP+A*KD+RuleKD 均使用 8 个预先固定、彼此独立且匹配的训练
  seed：`7/17/27/37/47/57/67/77`；不得用增加评估 episode 代替独立训练 seed；
- MAPPO-WP+A*KD+ShuffleKD 与 MAPPO-NoWP 使用预先固定的 `7/17/27` 三个诊断 seed，只允许描述
  效应方向、方差和失败模式，不进行显著性主张；
- checkpoint 选择必须采用预先定义的规则，不得事后选择表现最好的单一模型；
- 同时报告 episode 数、总环境步数、训练时间和学习曲线；
- 正式运行前用 pilot 判断预算是否足以达到平台期，必要时统一调整所有组的预算。
- G3 先冻结 G4 pilot 的步数和正式预算选择规则；G4 完成后只允许为全部正式学习组
  写入一个正式步数值，并在 G5 前形成协议增补，不得为不同组选择不同预算。

### 6.4 最终评估

- 每个训练 seed 使用未参与 G2 校准的 `200–209` 共 10 个评估 seed，每个20回合；
- 评估动作采用固定的确定性或随机策略设置，并在所有组保持一致；
- 至少进行不同 AGV 数量、任务到达强度、初始电量和拥堵/冲突条件的鲁棒性测试；
- `1.20/0.20/0.80` 固定为所有核心组共同使用的 evaluation-only 能源压力场景，不作为
  核心训练环境；
- 除训练使用的 medium 布局外，至少冻结两个不参与训练和 G2/G4 调参的未见布局：
  一个窄通道布局和一个中心瓶颈/交叉通道布局；地图文件、生成规则及哈希必须在 G3 固定；
- 未见布局首先作为 evaluation-only 零样本泛化证据；如因观测维度或环境契约无法直接
  迁移，必须在 G3 前修复统一接口，不得观察结果后重新设计有利布局；
- 每个最终实验至少保留一次确定性可视化回放和可追溯诊断日志。

### 6.5 指标

**任务与效用指标**：

- 唯一主要效用指标：每 1000 environment steps 完成任务数；
- 主要样本效率指标：按 environment steps 对学习曲线计算的 AUC；
- 累计 team reward；
- 每回合完成任务数和各 AGV 完成任务数；
- 任务完成率、吞吐量、makespan、每回合步数；
- A/B/C 等优先级任务完成时延；
- 任务饥饿率、最大等待时间或未完成任务年龄。

**安全与协调指标**：

- 碰撞率和能量死亡率作为主要安全约束；完整方法的效用收益不得以安全显著退化为代价；
- 碰撞数与碰撞率；
- 终止死锁率、状态重复和路径活锁；
- AGV 死亡数、低电量失败和充电站拥堵；
- 受控场景中的让行、优先通行和返充行为成功率。

**效率指标**：

- 达到预设性能阈值所需的环境交互步数；
- 墙钟训练时间、环境步吞吐、显存和内存占用；
- 单步推理时间；
- A* 调用次数、重规划次数、规划失败/部分路径率、搜索耗时 P50/P95/P99、扩展节点数、
  缓存命中率及完整控制循环时延；
- 训练和执行期间在线 LLM API 调用次数，目标为 0。

累计 reward 与完成任务数必须分别记录。reward 包含奖励塑形和惩罚，不能作为实际
任务吞吐的替代指标。

### 6.6 统计规范

- 报告 `mean ± standard deviation` 和 95% 置信区间；
- 训练 seed 是学习方法比较的独立统计单位；同一 checkpoint 的评估 seed 和 episode
  只用于降低该 checkpoint 的估计噪声，不得伪装成独立训练样本；
- 根据分布与配对结构选择适当的显著性检验，并同时报告效应量；
- 主要确认性比较及其假设族必须在 G3 冻结，并使用 Holm 方法控制多重检验风险；
- 当前确认性假设族至少覆盖：完整方法对 MAPPO-WP、A*KD 主效应、LLMKD 主效应、
  完整方法对 QMIX-WP，以及完整方法对 MAPPO-WP+A*KD+RuleKD；效应方向、统计量和
  安全非退化判据必须在正式结果生成前写入协议；
- 8 个训练 seed 是当前配对精确置换检验和多重校正下的最低正式预算，不得降回 5 个；
- 在正式实验前固定失败训练、缺失日志、异常值和提前终止的处理规则；
- 不得只展示最佳 seed、只报告显著指标或隐藏安全失败。

### 6.7 LLM 教师质量实验

在同一批受控场景上比较 DeepSeek-V4-Flash、DeepSeek-V4-Pro、GPT-5.6 Terra
和 GPT-5.6 Luna，至少评估：

- schema 合法率、分数稳定性和重复调用一致性；
- 与冻结规则和场景方向的一致率；
- 自动审计问题数与人工盲审评分；
- 标签分布、模型间一致性和分歧案例；
- 每条标签耗时、token 使用量和成本。

主训练默认继续使用已审计的 DeepSeek-V4-Flash 400 条数据。只有教师差异显著且
主线时间充足时，才增加不同教师标签驱动的训练实验。

除模型间标签质量审计外，必须在至少 100 个不参与标签训练和提示词调试的冻结状态上，
由两名评价者独立盲审 LLM 标签与规则标签，报告一致率、评价者间一致性和典型分歧。
MAPPO-WP+A*KD+RuleKD 与 MAPPO-WP+A*KD+ShuffleKD 必须保持与 LLMKD 相同的网络宽度、语义损失接口和
训练预算边界，从而区分“LLM 状态相关知识”与“任意辅助监督/额外损失”的作用。

## 7. 阶段门与 Go/No-Go 标准

本章只定义 G0–G7 的阶段验收标准，不承担任务拆解。第 8 章负责给出执行步骤，并与
根目录 `TASKS.md` 通过任务 ID 一一对应。只有第 8 章对应任务全部完成、下列通过条件
全部满足且证据可核验时，Gate 才可标记为通过；单次训练完成或单项指标达标不能替代
阶段验收。

### Gate G0：当前训练产物审计

**通过条件**：800-episode 运行的必要产物完整且相互一致；不存在 NaN、Inf、异常退出
或在线 LLM 调用；审计结论被明确限定为可行性证据，不被用于正式方法比较。

### Gate G1：加载—评估—回放闭环

**通过条件**：Phase 3 单语义与 Phase 4 双语义 checkpoint 均可正确加载；真实 Phase 4
checkpoint 可完成评估和确定性回放；相关脚本、回归测试和零在线 LLM 证据完整。

### Gate G2：环境风险复核

**通过条件**：A* 终点预约风险与充电风险均有定量诊断；A* 修复通过共享终点、显式等待、
持续占用、顶点冲突和对向边交换的确定性安全门，并由项目负责人完成 seed `300–309 ×
20 episodes` 的 legacy/fixed 配对诊断；必要修改通过完整回归；
充电暴露、到站、等待、恢复、最低电量和能量死亡可被可靠记录；候选能耗倍率与进入/释放
双阈值经过同配置小规模重训练验证。未解决风险必须形成可追踪的限制说明，否则不得进入
正式协议冻结。

### Gate G3：正式协议冻结

**通过条件**：存在唯一、可版本追踪的正式实验清单，完整冻结代码、核心四组、QMIX-WP、
MAPPO-WP+A*KD+RuleKD、两个诊断组和端到端启发式基线的配置与公平边界，以及环境契约、8 个
正式训练 seed、诊断 seed、两个未见布局及哈希、标签数据、盲审样本、主要指标与假设族、
G4 pilot 预算、正式预算选择规则、评估协议、充电参数、安全边界、日志 schema、checkpoint
选择和失败处理规则；聚合与绘图脚本不得继续硬编码五 seed 矩阵。G4 后只允许按预注册规则
写入一个正式训练预算值。冻结后不得针对单组结果进行修改。

### Gate G4：小规模 pilot

**通过条件**：核心四组在相同 seed 和环境交互预算下端到端运行稳定；QMIX-WP、
MAPPO-WP+A*KD+RuleKD、MAPPO-WP+A*KD+ShuffleKD、MAPPO-NoWP、端到端启发式基线和两个未见布局均通过
短预算/短评估链路 smoke；产物、资源使用和评估链路完整；核心场景充电暴露可比且安全
指标不越界，统一能源压力场景能覆盖高暴露行为；能够依据统一规则确定正式训练预算。

### Gate G5：正式多种子训练

**通过条件**：四个核心组、QMIX-WP 和 MAPPO-WP+A*KD+RuleKD 均完成 8 个有效匹配训练 seed；
MAPPO-WP+A*KD+ShuffleKD 和 MAPPO-NoWP 均完成 3 个固定诊断 seed；所有运行遵守冻结协议，失败运行
仅按预注册规则处理；不存在结果导向的单组超参数、基线替换或预算变更。

### Gate G6：独立评估与统计

**通过条件**：核心组与正式比较基线完成冻结的未见 seed 主评估、两个未见布局零样本
泛化、负载/规模/能源鲁棒性评估、LLM/规则标签盲审和 A* 运行时诊断；主要指标、统计量、
置信区间、效应量、安全结果和失败案例齐全；诊断组不被用于越界显著性主张；每项拟写入
论文的主张均可追溯到证据或明确限制。

### Gate G7：论文就绪

**通过条件**：全部论文图表由版本化脚本从冻结原始日志生成；方法、配置、代码、表格和
图中的术语与数值一致；内部证据审查通过，英文论文可以据此定稿。

## 8. 六至八周 Roadmap

本章是项目的阶段任务步骤指引。下列任务 ID、顺序和含义必须与根目录 `TASKS.md`
的 Gate 跟踪区保持一一对应；完成状态只在 `TASKS.md` 中维护。周次是计划窗口，不改变
Gate 的先后依赖或第 7 章的验收要求。

### G0：当前训练产物审计（第 1 周）

- **G0-1**：检查 800-episode 运行的 `summary.json`、`episodes.csv`、`updates.csv`、
  `checkpoint_final.pt` 和 TensorBoard 事件文件；
- **G0-2**：核验无 NaN、Inf、异常退出、必要产物缺失和在线 LLM 调用，并记录碰撞、
  A* 诊断计数及历史 checkpoint 路径问题；
- **G0-3**：将结果限定为 G0 可行性结论，不作为正式性能结论。

### G1：加载—评估—回放闭环（第 1 周）

- **G1-1**：修复 Phase 4 `semantic_dim=2` checkpoint 加载，兼容旧张量推断并在新
  checkpoint 中持久化语义维度；
- **G1-2**：完成 Phase 3 单语义、Phase 4 双语义和不兼容 checkpoint 回归测试；
- **G1-3**：使用真实 Phase 4 checkpoint 完成未见 seed 单回合评估与确定性回放，保留
  评估 JSON、轨迹、回放摘要和机器可读审计；
- **G1-4**：确认 `eval/evaluate_phase3.py` 与 `visualize.py` 正确处理 Phase 4 checkpoint。

###  G2：环境风险复核（第 2 周）

- **G2-1**：按第 4.3.1 节处理 A* 终点预约持续整个 horizon 引起的伪无解和活锁：
  - **G2-1a**：冻结共享终点、临时无路、显式等待、持续占用及边交换最小复现场景和指标；
  - **G2-1b**：实现带首动作/到达状态的时空结果、显式 `NOOP`、两 step 终点保留和按
    物理状态区分的占用期限；
  - **G2-1c**：完成确定性安全门、适用回归和短时 smoke，不启动训练或大规模评估；
  - **G2-1d**：由项目负责人运行 legacy/fixed 的 seed `300–309 × 20 episodes` 配对诊断；
  - **G2-1e**：审计长时产物，按预注册阈值决定采用修复或保留 legacy 并记录限制；
- **G2-2**：使用已验收的 800-episode checkpoint 和定向回放检查返充、拥堵、AGV 死亡
  与任务中断；
- **G2-3**：实现可配置耗电倍率与充电滞回，完成预定及细化压力组合诊断；若旧 checkpoint
  无合格组合，则记录结论并转入同配置重训练；
- **G2-4**：补齐低电量触发、充电目标、到站、充电等待、实际充电、任务恢复、最低电量
  和能量死亡指标；
- **G2-5**：充电或预约行为修改后，运行适用安全门与完整回归测试；
- **G2-6**：记录未解决风险、证据边界及其对论文主张和后续 pilot 的限制。

### G3：正式协议冻结（第 2 周）

- **G3-1**：冻结代码提交、四组配置、环境契约、标签数据哈希、正式训练 seed
  `7/17/27/37/47/57/67/77`、诊断 seed `7/17/27`、G4 等环境步数 pilot 预算、正式预算
  选择规则和 `10 seeds × 20 episodes` 未见 seed 评估协议；
- **G3-2**：将执行期 A* waypoint 与训练期 A* KL 冻结为独立因子，并采用宪章定义的四组名称；
- **G3-3**：冻结耗电倍率、充电率、进入/释放双阈值、任务中断/恢复语义和规则层安全边界；
- **G3-4**：冻结日志 schema、运行清单、checkpoint 选择、失败重试及正式数据/绘图流水线，
  将聚合、表格和绘图数据契约由五训练 seed 更新为八训练 seed；
- **G3-5**：冻结 QMIX-WP、MAPPO-WP+A*KD+RuleKD、MAPPO-WP+A*KD+ShuffleKD、MAPPO-NoWP 和
  Heuristic-Dispatcher+A* 的实现边界、预算、公平性及允许主张；
- **G3-6**：实现并冻结窄通道和中心瓶颈/交叉通道两个未见布局、地图哈希、统一接口和
  evaluation-only 规则；
- **G3-7**：冻结主要效用/样本效率指标、安全约束、确认性假设族，以及至少 100 个状态、
  两名评价者的标签盲审协议。

### G4：小规模 Pilot（第 3 周）

- **G4-1**：四个核心组使用相同小规模环境交互预算和匹配 seed 完成端到端运行；
- **G4-2**：检查各组损失、吞吐、显存、日志、checkpoint、评估脚本和零在线 LLM 合规性；
- **G4-3**：确认四组在核心场景获得可比的低电量/充电暴露，并在统一能源压力场景覆盖
  高暴露行为，通过到站、任务恢复、能量死亡和充电拥堵安全检查；
- **G4-4**：依据核心四组匹配学习曲线确定一个适用于全部正式学习组的交互预算，在 G5
  前写入协议增补，不进行分组事后调参；
- **G4-5**：对必需外部/规划基线、语义对照、NoWP 诊断、两个未见布局、八 seed 聚合与
  标签盲审数据入口完成端到端 smoke，失败时只能采用 G3 预注册替代方案。

### G5：正式多种子训练（第 4–5 周）

- **G5-1**：完成四个核心组、QMIX-WP 和 MAPPO-WP+A*KD+RuleKD，每组 8 个匹配训练 seed；
- **G5-2**：核验全部运行遵守冻结协议，失败运行只按预注册规则处理；
- **G5-3**：确认观察结果后未对任何单组实施结果导向的超参数、基线选择或预算变更；
- **G5-4**：完成 MAPPO-WP+A*KD+ShuffleKD 与 MAPPO-NoWP 各 3 个固定诊断 seed，只作机制和
  失败模式分析。

### G6：独立评估与统计（第 6–7 周）

- **G6-1**：完成核心组、QMIX-WP、MAPPO-WP+A*KD+RuleKD 和端到端启发式基线的未见 seed
  主评估，以及全部规定组的两个未见布局与负载/规模/能源鲁棒性评估；
- **G6-2**：生成统计汇总、置信区间、效应量、优先级/任务指标、安全结果和代表性失败案例；
- **G6-3**：将每项论文主张映射到支持证据或明确限制，再开始结果章节定稿；
- **G6-4**：完成至少 100 个冻结状态的双人标签盲审、规则/LLM/扰乱标签分析，以及 A*
  调用、失败、扩展节点、缓存和 P50/P95/P99 时延审计。

### G7：论文就绪（第 8 周）

- **G7-1**：通过版本化脚本从冻结原始日志生成全部论文图表；
- **G7-2**：审计方法文本、配置、代码、表格和图中的术语及数值一致性；
- **G7-3**：完成内部复核，再将结论写入英文论文。

## 9. 可复现性、数据与 Git 规范

- 每次运行记录代码提交、配置、seed、环境 ID、数据哈希、设备和完整命令；
- 训练与评估必须提供 TensorBoard 实时监控命令和产物目录；
- 原始日志只追加或新建，不手工覆盖；汇总数据可重建；
- 图表必须由脚本从 CSV/JSON 等真实数据生成，并同时保留适合论文的矢量格式；
- 模拟数据仅可用于规划，文件名必须标注 `mock_` 或 `synthetic_`，不得写成真实结果；
- 重大算法改动、实验协议冻结和阶段门通过后创建聚焦的 Git checkpoint；
- 不清理、回滚或覆盖用户已有未提交修改；禁止破坏性 Git 操作；
- 提交只包含当前任务范围内的文件，训练产物和密钥不得进入 Git。
- 训练、多 seed 评估、长回放和其他明显耗时任务由项目负责人运行；代码协作方负责实现、
  短时测试、命令准备和产物审计，未经项目负责人再次明确要求不得代为启动长任务。

## 10. 风险与降级策略

| 风险 | 影响 | 首选处理 | 降级策略 |
|---|---|---|---|
| 800 episodes 未收敛 | 正式预算不足 | 用学习曲线统一增加交互步数 | 报告样本效率和未收敛限制 |
| A* 终点预约造成伪无解 | 活锁、额外计算 | 有界终点保留、显式 NOOP、区分部分路径并重跑配对安全门 | 保留 legacy 并量化为限制 |
| 充电机制长时失效 | AGV 死亡、任务中断 | 增加长时诊断与受控评估 | 不扩大主张，单列失败分析 |
| 四组定义不公平 | 消融结论无效 | 冻结 waypoint 与 KL 的独立因子 | 使用准确命名并增加 NoWP 诊断 |
| 多种子训练超时 | 统计功效不足 | 优先八 seed 正式组并统一缩减交互预算 | 删除条件性扩展，不把正式组降回五 seed |
| QMIX-WP 无法公平接入 | 缺少外部算法证据 | 在 G4 前修复统一接口 | 按 G3 预注册规则改用 IPPO-WP 或 VDN-WP，不得省略外部基线 |
| LLM 标签可被简单规则替代 | LLM 贡献不成立 | 完成 RuleKD、扰乱标签和盲审 | 收缩为辅助语义监督结论，不夸大 LLM 独特性 |
| 单一仓库场景过拟合 | 方法说服力不足 | 完成两个未见布局及负载/规模泛化 | 只允许收缩主张，不得删除布局后维持泛化结论 |
| 在线加急指令解析错误或超时 | 错误换级、执行抖动 | 原子规则校验、审计日志和确定性降级 | 关闭在线入口，保留结构化人工调整 |

## 11. 优先级原则

当时间、算力或工程复杂度发生冲突时，优先级固定为：

1. 正确性、安全性与无数据泄漏；
2. 核心四组、QMIX-WP 与 MAPPO-WP+A*KD+RuleKD 的八 seed 公平实验；
3. 两个未见布局、端到端启发式基线、MAPPO-WP+A*KD+ShuffleKD、MAPPO-NoWP 与 A* 运行时审计；
4. 独立评估、统计分析、标签盲审和失败案例；
5. 可复现日志、图表与论文证据追踪；
6. 多 LLM 教师质量与成本比较；
7. CBS、自适应教师加权、执行期自然语言加急、学习式充电、Linux 迁移和其他扩展。

任何扩展不得挤占核心证据链所需的时间和算力。
